#include<bits/stdc++.h>
using namespace std;
int main()
{

     queue<int> q;
     q.push(10);
     if(!q.empty())
     {
        q.pop();
     }






     //Must check for pop back front,is the queue empty or not  


    return 0;
}